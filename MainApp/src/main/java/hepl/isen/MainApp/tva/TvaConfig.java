package hepl.isen.MainApp.tva;

import org.springframework.context.annotation.Configuration;

@Configuration
public class TvaConfig {
/*
    @Bean
    CommandLineRunner commandLineRunner(TvaRepository repository) {
        return args -> {
            Tva tvaLivres = new Tva(
                    6
            );
            Tva tvaAutres = new Tva(
                    21
            );

            repository.saveAll(List.of(tvaLivres, tvaAutres));
        };
    }
 */
}
